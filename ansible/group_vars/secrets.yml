---
# PLACEHOLDER — replace with ansible-vault encrypted file.
#
# Create:  ansible-vault create ansible/group_vars/secrets.yml
# Edit:    ansible-vault edit ansible/group_vars/secrets.yml
#
# Required variables:
#   db_password: "<generate>"
#   better_auth_secret: "<generate, 32+ chars>"
#   ghcr_token: "<github PAT with read:packages scope>"
#   cloudflare_api_token: "<cloudflare API token>"
