{% for domain in domains %}
{% if domain.upstream is defined %}
{{ domain.host }} {
    reverse_proxy {{ domain.upstream }} {
        health_uri /health
        health_interval 5s
        lb_try_duration 10s
    }
}
{% endif %}
{% endfor %}
